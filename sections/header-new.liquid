{{ 'header.css' | asset_url | stylesheet_tag }}
<div class="container">
  <div class="header_top">
    <div class="logo">
      <a href="/">
        <img src="{{ section.settings.logo | img_url : "master" }}" alt="logo">
        <span>{{ section.settings.logo_txt }}</span>
      </a>
    </div>
    <div class="search_box">
      <div class="search">
<!--         {{ routes.search_url }}  -->
<!--         /pages/search-fridge? -->
        <form action="{{ routes.search_url }}" method="get" role="search" class="search-form earch search-modal__form">
          <div class="field">
            <div id="searchajaxico"></div>
            <!--   <label class="field__label" for="Search-In-Modal-1">{{ 'general.search.search' | t }}</label> -->
            <input class="search-input ui-autocomplete-input" 
                   id="Search-In-Modal-1"
                   type="search"
                   name="q"
                   value=""
                   placeholder="{{ 'general.search.search' | t }}"
                   {%- if settings.predictive_search_enabled -%}
                   role="combobox"
                   aria-expanded="false"
                   aria-owns="predictive-search-results-list"
                   aria-controls="predictive-search-results-list"
                   aria-haspopup="listbox"
                   aria-autocomplete="list"
                   autocorrect="off"
                   autocomplete="off"
                   autocapitalize="off"
                   spellcheck="false" 
                   {%- endif -%}
                   >

<!--             <input type="hidden" name="options[prefix]" value="last"> -->
            <button class="btn submit" aria-label="{{ 'general.search.search' | t }}">
              <img src="{{ 'icon_bold.png' | asset_url  }}" alt="">
            </button> 
          </div>
        </form>
      </div>
    </div>
    <div class="header_right dvdvf">
      <ul>
        {% for block in section.blocks %}
        {% if forloop.index == 3 %}
     
        
        <li {% if block.settings.menu_txt == 'Cart' %}id="CartCount"{% endif %}>
           {% if customer %}
          <a href="/account/logout">
          <img src="{{ block.settings.icon | img_url : 'master' }}" alt="">
             <span></span>
            Log out
           </a>
          {% else %}
          <a href="{{ block.settings.menu_url }}">
          <img src="{{ block.settings.icon | img_url : 'master' }}" alt="">
             <span></span>
          {{ block.settings.menu_txt }} 
           
          </a>
          {% endif %}
        </li>
     
        
        {% else %}
        <li {% if block.settings.menu_txt == 'Cart' %}id="CartCount"{% endif %}>
          <a href="{{ block.settings.menu_url }}">
          <img src="{{ block.settings.icon | img_url : 'master' }}" alt="">
             <span></span>
          {{ block.settings.menu_txt }} 
           
          </a>
        </li>
        {% endif %}
        {% endfor %}        
      </ul>
    </div>
  </div>
  <div class="mobile_view">
    <div class="search_box_ll">
      <div class="bar13" onclick="myFunction(this)">
        <div class="bar1"></div>
        <div class="bar2"></div>
        <div class="bar3"></div>
      </div>
      <div class="search">
        <form action="{{ routes.search_url }}" method="get" role="search" class="search-form earch search-modal__form">
          <div class="field">
            <div id="searchajaxico"></div>
            <!--   <label class="field__label" for="Search-In-Modal-1">{{ 'general.search.search' | t }}</label> -->
            <input class="search-input ui-autocomplete-input" 
                   id="Search-In-Modal-1"
                   type="search"
                   name="q"
                   value=""
                   placeholder="{{ 'general.search.search' | t }}"
                   {%- if settings.predictive_search_enabled -%}
                   role="combobox"
                   aria-expanded="false"
                   aria-owns="predictive-search-results-list"
                   aria-controls="predictive-search-results-list"
                   aria-haspopup="listbox"
                   aria-autocomplete="list"
                   autocorrect="off"
                   autocomplete="off"
                   autocapitalize="off"
                   spellcheck="false" 
                   {%- endif -%}
                   >

            <input type="hidden" name="options[prefix]" value="last">
            <button class="btn submit" aria-label="{{ 'general.search.search' | t }}">
              <img src="{{ 'icon_bold.png' | asset_url  }}" alt="">
            </button> 
          </div>
        </form>
      </div>
    </div>
  </div> 

</div>

<style>
.bar13 {
  display: inline-block;
  cursor: pointer;
}

.bar1, .bar2, .bar3 {
  width: 35px;
  height: 5px;
  background-color: #333;
  margin: 6px 0;
  transition: 0.4s;
  display:block!important;
}

/* .change .bar1 {
  -webkit-transform: rotate(-45deg) translate(-9px, 6px);
  transform: rotate(-45deg) translate(-9px, 6px);
}

.change .bar2 {opacity: 0;}

.change .bar3 {
  -webkit-transform: rotate(45deg) translate(-8px, -8px);
  transform: rotate(45deg) translate(-8px, -8px);
} */
  
  
 
  

  
  
</style>

{% schema %}
{
"name": "Header",
"settings": [
{
"type": "image_picker",
"id": "logo",
"label": "LOGO"
},
{
"type": "text",
"id": "logo_txt",
"label": "Logo Text"
}
],

"blocks": [
{
"type": "link",
"name": "Link",
"settings": [
{
"id": "icon",
"type": "image_picker",
"label": "Icon Image"
},
{
"id": "menu_txt",
"type": "text",
"label": "Menu Text"
},
{
"id": "menu_url",
"type": "url",
"label": "Menu Url"
}
]
}
]
}
{% endschema %}
{% stylesheet %}
{% endstylesheet %}
{% javascript %}
{% endjavascript %}
